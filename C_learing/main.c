#include <stdio.h>
#include <ctype.h>
#define EOF '|'

int
main(void)
{
    char c;
    long char_c=0, word_c=0, sentence_c=0;
    while((c=getchar()) != EOF)
    {
        if (c != ' ')
            char_c++;
        else:
    }

}
